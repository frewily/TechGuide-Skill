# TechGuide Skill 设计文档

> **目标读者：** 实现此 Skill 的 AI 代理（或人）。本文档是唯一规格来源，实现时以本文档为准，不猜测、不擅自扩展。
>
> **姊妹项目：** [RepoGuide](https://github.com/frewily/RepoGuide-Skill)（学"已有项目"）。本 Skill 与其是姊妹关系，独立仓库、独立发布，仅在笔记体系和入口模式上保持一致。

---

## 1. 概述

**TechGuide** 是一个跨平台 Agent Skill（兼容 Trae、Codex、Claude Code），帮助开发者**学习一门新技术**——例如"如何写爬虫""如何写自动化脚本"。它通过引导式对话完成从概念理解到动手实践的完整学习，并同步产出结构化 Markdown 笔记（可导入 Obsidian）。

与 RepoGuide 的定位差异：

| | RepoGuide | TechGuide |
|---|---|---|
| 学什么 | 一个已有的项目/代码库 | 一门技术/技能（无代码库） |
| 输入 | 本地路径、仓库 URL、文件、代码片段 | 自由主题（如"我想掌握爬虫"） |
| 证据来源 | 文件、配置、测试 | 权威文档、可运行的示例、用户亲手写的练习 |
| 练习 | 可选（第 4 阶段实践闭环） | **主线**（贯穿核心阶段） |
| 学习产出 | 能修改项目、审阅 Agent 改动 | 能独立写出应用该技术的小项目 |

## 2. 要解决的问题

- 面对一门新技术，不知道从哪学起、学什么、学到什么程度算"会了"。
- 看教程容易"一看就会，一写就废"——缺少动手练习的强引导。
- 学习结论散落在对话里，没有沉淀成可复用的知识。

## 3. 与 RepoGuide 的关系（重要）

- **独立仓库、独立 Skill 包**，可单独安装到宿主工具。
- **保持一致的部分**（照搬思路，不复制文件）：
  - 跨平台入口模式：`SKILL.md` / `skill.json` / `instructions.md` 三层结构，教学规则只放 `instructions.md`。
  - 笔记体系：5 份笔记 + 同名结构 + Obsidian front matter，保证两份 Skill 产出的笔记在同一 Obsidian vault 里风格统一。
  - `init-notes.sh` 的实现思路：安全校验、不覆盖已有笔记、原子写入、跨平台。
- **必须重写的部分**（不要沿用 RepoGuide 的内容）：
  - `instructions.md` 正文（工作流不同）。
  - 5 份模板的具体内容（面向"技术学习"而非"项目学习"）。
  - 练习设计（从"可选实践闭环"升级为"学习主线"）。

## 4. 学习理念

**"学以致用"**——学习的终点不是"看懂了"，而是"能独立写出来"。

优先级（从高到低）：

1. **概念本质**——这门技术的核心概念是什么？解决什么问题？各概念之间什么关系？
2. **亲手实践**——写最小示例、完成小练习，验证理解。
3. **常用模式**——这门技术最常见的落地写法/套路。
4. **完整 API 参考**——只覆盖实际用到的，不背诵。

**不做什么：**

- 不逐条念文档/API 手册。
- 不编造不存在的 API、库或"最佳实践"。
- 不把学习变成被动听讲——每个核心概念都要配一个动手验证。

## 5. 证据纪律（与 RepoGuide 不同，务必落实）

RepoGuide 的证据是代码文件；TechGuide 没有代码库，证据只能来自：

1. **权威来源**：官方文档、语言/框架官方教程，标注出处链接。
2. **可运行的示例**：Agent 给出的示例必须实际可运行，不得凭空编造。
3. **用户亲手写的练习结果**：用户贴出的代码/运行输出。

**禁止：**

- 编造 API 签名、库名、配置项。
- 说"这是最佳实践"却不给出处。
- 把"我记得"当"事实"——不确定就查（WebSearch / WebFetch）或明确标注"需验证"。

每个实质结论都要标注：**证据**（出处/可运行示例）还是**推断**（待验证）。

## 6. 工作流程（五阶段）

整体遵循：**每完成一个阶段或一个重要模块，暂停确认，再继续**；用户可随时提问或调整深度。

### Phase 1：目标与技术范围确认

- 确认主题（如"写爬虫"）、用户当前基础、学习目标（学完能独立做出什么）。
- 缩小范围：一门技术通常很宽，先圈定"本次要掌握的子集"（如"静态网页爬取 + 反爬处理"）。
- 产出笔记：`技术全景.md` 的"学习目标"、"已有基础"。

### Phase 2：知识地图

- **先确认领域划分**：把概念按"知识领域"分组。先给出默认骨架（基础概念 → 核心 API → 常用模式 → 生态/工具 → 常见坑），询问用户是否按此划分；用户无异议则采用默认，有异议则按用户自定义调整。领域划分决定后续 Phase 3–4 的教学顺序与笔记章节结构。
- 列出这门技术由哪些核心概念组成，按领域分组，画出概念关系图（Mermaid flowchart 或 mindmap）。
- 每个概念标注：是什么、所属领域、与相邻概念的关系。
- 用用户已会的技术做类比（如：爬虫 ≈ "浏览器手动操作" 的自动化）。
- 产出笔记：`技术全景.md`（知识地图 + 领域清单）、`核心概念词典.md`（概念清单初版）。

### Phase 3：概念讲解 + 示例

**按领域逐个讲解**（顺序遵循 Phase 2 领域清单的学习顺序），每个领域内的概念按固定结构：

1. **是什么**——一句话定义 + 解决什么问题。
2. **为什么**——为什么这样设计？替代方案取舍。
3. **怎么做**——最小可运行示例（Agent 负责给，且保证能跑）。
4. **动手验证**——请用户自己改写一个参数或加一个小变化，确认理解。

每讲完一个领域暂停，问用户："这部分有疑问吗？"再继续下一个领域。

- 产出笔记：`主题笔记.md`（按领域分章）、`核心概念词典.md`（持续补充）、`速查表.md`（遇到 API/命令即记录）。

### Phase 4：动手练习（核心，不可跳过、不可降级为可选项）

- 设计 **2–3 个小练习**，由易到难，全部落在"刚学完的概念"范围内。
- 流程：用户先自己写 → Agent 不直接给答案，先点评思路、指出问题 → 用户改 → 对照讲解正确做法与差异 → 给一个进阶练习。
- 练习必须：可独立完成、可验证（能运行/能测试）、范围可控。
- 若用户卡住，Agent 用提示引导（如"试试这个 API 的文档""想想这里边界条件是什么"），不要直接给整段代码。
- 产出笔记：`主题笔记.md` 对应领域的"练习记录"小节（练习题目、用户写法、正确做法、差异要点、验证方式）；练习记录**分散到各领域章节末尾**，不集中汇总。

### Phase 5：知识沉淀

- 汇总本次学习：已掌握的概念、仍存的疑问、可复用的模式/套路。
- 把"速查表"整理成用户自己的 API/命令参考。
- 产出笔记：`个人总结.md`，并回填修正前面各笔记。

## 7. 输入处理

典型触发：

- "我想掌握如何写爬虫"
- "学习自动化脚本，目标是能写定时任务"
- "帮我系统学一下 X 技术，我完全没基础"

**处理规则：**

- 输入是**自由主题** → 直接进入 Phase 1。
- 输入里给了**代码库路径/URL** → 提示用户：这属于 RepoGuide 的职责，建议改用 RepoGuide；若用户坚持，可按"以该项目的实现为例"的方式降级处理，但主线仍是技术学习而非项目导读。
- 主题太宽（如"学 Python"）→ 先和用户确认具体目标（学 Python 干什么），再圈定范围。

## 8. 笔记规范

### 目录与文件

笔记输出到**调用目录**下的 `notes/{topic-name}/`：

```text
notes/
└── {topic-name}/
    ├── 技术全景.md        # 学习目标、知识地图（Mermaid）、概念关系
    ├── 核心概念词典.md    # 概念/术语/API 的要点说明
    ├── 主题笔记.md        # 分主题的讲解、示例、练习记录
    ├── 个人总结.md        # 用户自己的理解、疑问、类比、TODO
    └── 速查表.md          # 命令、API、配置、常见坑
```

### front matter 规范（与 RepoGuide 一致，保持 vault 统一）

```yaml
---
title: 技术全景
topic: "{{topic-name}}"
created: {{date}}
updated: {{date}}
tags: [tech-guide, learning]
---
```

`tags` 用 `tech-guide`（区分于 RepoGuide 的 `repo-guide`），便于 Obsidian 检索。

### 模板章节要求

- **技术全景.md**：学习目标 / 已有基础 / **领域清单**（领域 | 包含概念 | 学习顺序）/ 知识地图（Mermaid）/ 概念关系表 / 待验证的推断。
- **核心概念词典.md**：概念表（概念 | 一句话定义 | **所属领域** | 证据出处 | 我的熟悉程度）+ **按领域分节**的要点章节（是什么/为什么/关键用法/易错点）。
- **主题笔记.md**：**按知识领域分章**（每章 = 该领域的概念讲解 + 示例 + 动手验证 + **练习记录**小节：题目、用户写法、正确做法、差异、验证方式）；练习记录**分散到各领域章节末尾**，不集中汇总。
- **个人总结.md**：我现在的理解 / 已确认的证据 / 仍有疑问（checkbox）/ 下一步行动。
- **速查表.md**：**按领域分节**的 API/命令表 + 常见坑 + 排查入口。

每份模板用 `{{topic-name}}`、`{{date}}` 占位，由 `init-notes.sh` 替换。

## 9. 技术方案

### 文件结构

```text
tech-guide/
├── SKILL.md               # Codex / Claude Code 入口（只引用 instructions.md）
├── instructions.md        # 核心工作流（唯一教学规则源，本文档第 4–8 节的落地）
├── skill.json             # Trae 元数据入口
├── assets/templates/      # 5 份笔记模板
│   ├── 技术全景.md
│   ├── 核心概念词典.md
│   ├── 主题笔记.md
│   ├── 个人总结.md
│   └── 速查表.md
└── scripts/
    └── init-notes.sh      # 初始化笔记目录（复用 RepoGuide 的思路）
```

### 入口约定

| 平台 | 读取的入口 |
|---|---|
| Codex | `SKILL.md` |
| Claude Code | `SKILL.md` |
| Trae | `skill.json` |
| 所有平台 | `instructions.md`（共享工作流） |

`SKILL.md` 只含元数据 + 对 `instructions.md` 的明确引用；`skill.json` 保持 Trae 可识别的字段。所有教学规则只放 `instructions.md`，避免三入口漂移。

### init-notes.sh 要求（参考 RepoGuide 实现，不复制）

- 用法：`./init-notes.sh <topic-name>`
- 主题名校验：仅允许 `[A-Za-z0-9._-]`，拒绝 `.`、`..` 及路径穿越。
- 从**自身目录**定位 `assets/templates`（不依赖调用目录）。
- 用 `mktemp` + `sed` + `mv` 做占位符替换（`{{topic-name}}`、`{{date}}`），不依赖 macOS/GNU 特有的 `sed -i`。
- 已存在的笔记文件**不覆盖**。
- 兼容 macOS / Linux，`set -euo pipefail`。

### skill.json 建议

```json
{
  "name": "tech-guide",
  "displayName": "TechGuide - 技术学习私教",
  "description": "跨平台的技术学习工作流：输入一门技术主题（如爬虫、自动化脚本），通过概念讲解、动手练习和结构化笔记，帮助你真正掌握并运用它。",
  "triggerKeywords": ["学习技术", "掌握技术", "想学", "技术导读", "技能私教", "learn a technology", "tech guide"],
  "version": "0.1.0",
  "instructions": "instructions.md",
  "author": "TechGuide",
  "categories": ["learning", "education"]
}
```

## 10. 关键设计决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 载体 | 独立 Skill 包（独立仓库） | 触发清晰、可单独安装、互不影响迭代 |
| 输入 | 自由主题，无需代码库 | 学技术没有现成代码可读 |
| 练习 | 工作流主线（Phase 3–4 内嵌） | 技术学习必须动手，不能只看 |
| 证据 | 文档出处 / 可运行示例 / 练习结果 | 防幻觉、可复核 |
| 笔记体系 | 与 RepoGuide 同构（同名、同 front matter） | Obsidian vault 风格统一 |
| 笔记分层 | 按"知识领域"在章节级分层（保持 5 文件不变） | 概念不被平等对待、避免单文件膨胀，且不破坏 vault 统一 |
| 交互 | 引导式 + 问答式，每阶段暂停确认 | 节奏可控、不一次性倾倒 |

## 11. 验收标准

1. 三个入口（`SKILL.md`、`skill.json`、`instructions.md`）指向同一份工作流，且不重复教学规则。
2. 输入自由主题（无代码库）即可启动完整学习流程。
3. 练习是工作流主线：出现在 Phase 3（动手验证）与 Phase 4（小练习），**不是可选项**。
4. `init-notes.sh`：在临时目录运行能创建 5 份无占位符残留的笔记；二次运行不覆盖；非法主题名（`../x`、`.`、`..`）被拒绝且不写出 `notes/` 之外；macOS 与 Linux 均可运行。
5. 5 份模板均含 Obsidian front matter（`tech-guide` 标签）与全部必需章节；`主题笔记` 按领域分章，练习记录分散在各领域章节末尾。
6. 配套一个内容回归测试（如 `tests/` 下的 bash 脚本），断言：instructions.md 含练习主线、用户先写、证据纪律（禁止编造）等关键约束；模板含"练习记录"章节。
7. README 说明安装方式（三个平台入口）、触发示例、笔记输出位置。

## 12. 复用清单（从 RepoGuide 借鉴，不复制）

- 入口模式：`SKILL.md` / `skill.json` / `instructions.md` 三层，单点维护规则。
- `init-notes.sh` 的安全实现思路（校验、原子写入、不覆盖）。
- 笔记 front matter 风格与 5 文件结构。

**需完全重写：** `instructions.md` 正文、5 份模板内容、练习设计、触发词、README。
